# Card-Flip-Game

A small React + Vite memory card-flip game.

- Run with `npm install` then `npm run dev`.
- Built with React, Vite, and simple CSS.

See the project files in the `src/` directory for components and styles.
